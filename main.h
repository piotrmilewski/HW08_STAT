#ifndef MAIN
#define MAIN

void print_error( int );
char *prettyBytes( long unsigned, int, char * );
char getBitMode( int, int );
char *convertMode( int, char * );
char *lsifyPerms( int, char * );
int main();

#endif
